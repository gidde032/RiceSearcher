# Workflow open-questions ledger

Append-only. Record a question the first time the project makes a workflow choice
it cannot yet justify with evidence. Each stays OPEN until a real phase answers it.

- **OQ-1 (PARTIAL):** Numeric gate floors. **Coverage: RESOLVED for Phase 1** —
  skeleton measured 98.2%; floor set to 90% (headroom for later harder-to-test code),
  ratchet as it proves out. **Still OPEN:** scoring-token cap per episode and pull
  latency, which need the Phase-2 scorer + a real transcription run to measure.
