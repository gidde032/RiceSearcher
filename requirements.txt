# RiceSearcher — runtime dependencies.
# The heavy adapters are imported lazily; install them to enable those doors.
#
# ffmpeg is a SYSTEM dependency (used by yt-dlp and faster-whisper).

# Acquisition (D1) — the yt-dlp door:
yt-dlp>=2024.1.0

# Transcription (D3) — reuse RiceClipper's local stack:
faster-whisper>=1.1.1

# Scoring (D4, Phase 2) — the only paid-API dependency (used by `score`):
anthropic>=1.3.0

# Review UI (D7, Phase 4) — local FastAPI web server for `review` (never posts):
fastapi>=0.115.0
uvicorn>=0.30.0

# Dedup embeddings (D6, Phase 3) — local transcript embeddings for `dedup`.
# Heavy (pulls torch); imported lazily and swappable via the Embedder protocol,
# so the core + tests run without it. If it won't install on your Python, a
# lighter embedder can be dropped in behind ricesearcher.dedup.base.Embedder.
sentence-transformers>=2.2.0
